PORT=5000
NODE_ENV=development

# FIREBASE CONFIG
# To use Firebase, go to Firebase Console > Project Settings > Service Accounts
# Generate a new private key and paste its contents here as a JSON string
# Or point to the file path if using the firebase-admin Node.js setup
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "meditrack011", ...}'
